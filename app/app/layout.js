export const metadata = {
  title: "Geovana Acessórios",
  description: "Catálogo oficial",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
